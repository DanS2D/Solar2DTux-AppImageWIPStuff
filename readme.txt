
Run ./build.sh to build the AppImage.

The current issue is that the AppImage crashes when opening a file select dialog. 

That is the only known issue with the AppImage at present.
After that is fixed, it just needs file paths fixing to be relative as everything will be in the AppImage instead.
